[project]
name = "policy-collector-api"
version = "0.1.0"
description = "Expose merged markdown policies over FastAPI"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.116.0",
  "uvicorn>=0.35.0",
]

[tool.uv]
package = false
